#Reads dictionary file and makes dictionary

def read_dictionary():
    library = []
    with open('trajectory_library.txt','r') as infile:
        for line in infile:
            if line[0] == "{":
                library.append(eval(line))
    return library

print read_dictionary()
                
